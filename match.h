#ifndef VECTOR
#define VECTOR
#include<vector>
#endif

#ifndef HEADER
#define HEADER
#include"Header.h"
#endif

std::vector<std::pair<int, int> > match_points(std::vector<coordinate>&, std::vector<coordinate>&);